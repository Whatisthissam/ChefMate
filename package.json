{
  "name": "chefmaten-realtime-recipe-recommendation",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently -n server,client -c cyan,magenta \"PORT=5001 npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "seed": "DOTENV_CONFIG_PATH=server/.env node server/seed/seed.js",
    "test": "npm --prefix server test"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
